const initialState = {
  currentArtist: null,
  status: "idle",
};

const artistReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default artistReducer;
